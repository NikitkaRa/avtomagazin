JWT_KEY=
INTERNAL_KEY=
POSTGRES_PASSWORD=
ADMIN_EMAIL=
ADMIN_PASSWORD=
# Optional. Firebase service account JSON in one line, or a path inside the notifications container.
FCM_SERVICE_ACCOUNT_JSON=
FCM_SERVICE_ACCOUNT_PATH=
# Optional. Force JSON console logs outside Production (Production already uses JSON).
# Logging__Json=true

